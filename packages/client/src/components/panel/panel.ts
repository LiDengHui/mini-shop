import { defineComponent } from "vue";

export default defineComponent({
    name: "Panel",
    props: {
        title: {
            type: String,
        },
    },
    setup() {
        return {};
    },
});
